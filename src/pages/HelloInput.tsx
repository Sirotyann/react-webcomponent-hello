import React from 'react';

const HelloInputWrapper = () => {
    // const { name, type, value } = props;
    // const ref = useRef(null);

    // console.log('HelloInput ref', ref)

    return <div>abc</div>
}

export default HelloInputWrapper;